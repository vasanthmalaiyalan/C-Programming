#include<stdio.h>
int main()
{
    int j=4;
    (!j != 1 ? printf("Welcome\n"):printf("\nGood Bye\n")); //!= means same value is false but diffrent value is true and !j means if value is true this simpol(!)change false
}