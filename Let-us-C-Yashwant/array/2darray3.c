#include<stdio.h>

int main()
{
    int s[4][2]={
        {1234,43},
        {2345,56},
        {5432,32},
        {1345,56}
    };
    int i,j;

    for(i=0;i<=3;i++)
    {
        printf("\n");
        for(j=0;j<=1;j++)
           printf("%d\n",*(*(s+i)+j));
           printf("address of first row s = %p and &s[0][0] = %p\n",s,&s[0][0]);
           printf("address of second row %p\n",s+1);
           printf("*s = %p\n",*s);
           printf("**s = %d\n", **s);  
    }
}

/*
Expression	Points to
s	Row 0 முழுவதும் means s[0]
s+1	Row 1 முழுவதும் means s[1]
s+2	Row 2 முழுவதும் means s[2]
*(s)	{1234, 43} row means அப்போ *s என்ன?
*s


இது:

s point செய்யும் row-ஐ எடுத்துக் கொள்

அதாவது logically:

*s = {1234, 43}


ஆனால் இங்கே ஒரு முக்கியமான C rule இருக்கு 👇

⚠️ Array "decay" rule (மிக முக்கியம்)

C-ல்:

Array value-ஆ பயன்படுத்தப்பட்டால்,
அது தானாகவே
first element-ன் address ஆக மாறும்

அதாவது:

{1234, 43}  →  &s[0][0]


அதனால்:

*s  → &s[0][0]

அதனால்தான்:
Expression	Actually becomes
s	&s[0] (row address)
*s	&s[0][0] (first element address)
**s	1234 (value)
ஏன் *s print பண்ணினால் 1234 கிடைக்கிறது?

உதாரணம்:

printf("%d", **s);


Breakdown:

s   → row address like a[0][0] a[0][1]
*s  → element address like 
**s → value = 1234

Important correction to your understanding

நீ நினைத்தது:

"*s என்றால் முழு row கிடைக்க வேண்டும்"

ஆனா C-ல்:

Row (array) value-ஆ பயன்படுத்தப்படும்போது
அது pointer ஆக decay ஆகும்
அதனால் *s → first element address ஆக மாறுகிறது

*(*(s)+0)	1234 means (*s[0]
*(*(s)+1)	43
*/