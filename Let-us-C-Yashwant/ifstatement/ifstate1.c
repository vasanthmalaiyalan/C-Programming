#include<stdio.h>
int main()
{
    int num;
    printf("Enter the number less than 10: ");
    scanf("%d",&num);

    if(num <= 10)
    {
        printf("What an obedient servent you are !");
    }
}
