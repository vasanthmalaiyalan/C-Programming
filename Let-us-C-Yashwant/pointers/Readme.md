## இதை compare பண்ணிப் பார்ப்போம்
 

| Situation | Correct | Explanation |
| --------- | ------- | ----------- |
| `int *p = &i;` | ✅ | Pointer assign செய்ய normal |
| `printf("%p", &i);` | ⚠️ Works but warning வரலாம் | Compiler strict ஆக இருந்தால் warning |
| `printf("%p", (void*)&i);` | ✅ Perfect standard | C standard-க்கு fully correct |

Pointer என்பது
memory address-ஐ store செய்யும் variable.
`x = &a` என்றால் x-க்கு a-வின் address variable-laga store ஆகும்.
`*x` அந்த address-க்கு சென்று original value-ஐ access / modify செய்யும்.

int a = 10;
int *x = &a;

இதில் நடப்பது:

    a = 10
    → value 10 memory-ல் ஒரு address-ல் store ஆகிறது
    → அந்த address = &a (எ.கா: 0x567)

    x = &a
    → அந்த address (0x567) x-ன் value ஆக store ஆகிறது

    *x
    → x-க்குள் இருக்கும் value vaga errukum address (0x567)-க்கு சென்று
    → அந்த address-ல் இருக்கும் value (10)-ஐ access செய்கிறது

    *x = 20;
    → அந்த same address-ல உள்ள value மாற்றப்படுகிறது
    → அதனால் a இப்போது 20 ஆக மாறுகிறது

    a க்கு தனி address → 0x1000

👉 இப்போ முக்கியமான விஷயம்:

a க்கு தனி address → 0x1000

x க்கு தனி address → 0x2000

ஆனா x-ன் உள்ளே இருக்கிற value = &a = 0x1000